services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: dairymonitor
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-dairymonitor_local}
      POSTGRES_DB: dairymonitor
    ports:
      - "127.0.0.1:5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U dairymonitor"]
      interval: 5s
      retries: 10
volumes:
  postgres_data:
