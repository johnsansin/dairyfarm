'use client';
import {useCallback,useEffect,useState} from 'react';
import {History,Plus} from 'lucide-react';
import {api} from './api';
import SearchSelect from './search-select-v2';
type Followup={id:string;follow_up_date:string;status:string;notes:string;author:string;created_at:string};
export default function TaskFollowups({base,taskId,ur}:{base:string;taskId:string;ur:boolean}){const [rows,setRows]=useState<Followup[]>([]),[show,setShow]=useState(false),[busy,setBusy]=useState(false),[error,setError]=useState('');const load=useCallback(()=>api(`${base}/calendar/tasks/${taskId}/followups`).then(setRows).catch(e=>setError(e.message)),[base,taskId]);useEffect(()=>{void load()},[load]);async function save(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError('');try{await api(`${base}/calendar/tasks/${taskId}/followups`,{method:'POST',body:JSON.stringify(Object.fromEntries(new FormData(e.currentTarget)))});setShow(false);await load()}catch(e){setError((e as Error).message)}finally{setBusy(false)}}return <section className="task-followups"><div className="followups-head"><span><History size={16}/><strong>{ur?'فالو اپ ریکارڈ':'Follow-up history'}</strong><small>{rows.length}</small></span><button type="button" className="secondary-button" onClick={()=>setShow(value=>!value)}><Plus size={14}/>{ur?'فالو اپ شامل کریں':'Add follow-up'}</button></div>{error&&<div className="form-error" role="alert">{error}</div>}{show&&<form className="followup-form" onSubmit={save}><label className="form-field">{ur?'فالو اپ تاریخ':'Follow-up date'}<input name="follow_up_date" type="date" defaultValue={new Date().toLocaleDateString('en-CA')} required/></label><label className="form-field">{ur?'حالت':'Status'}<SearchSelect name="status" defaultValue="Pending"><option>Pending</option><option>In progress</option><option>Resolved</option><option>Needs another follow-up</option></SearchSelect></label><label className="form-field wide">{ur?'نوٹس':'Notes'}<textarea name="notes" rows={3} minLength={2} maxLength={4000} required/></label><div className="followup-actions"><button type="button" className="secondary-button" onClick={()=>setShow(false)}>{ur?'منسوخ':'Cancel'}</button><button className="button small" disabled={busy}>{busy?'…':ur?'محفوظ کریں':'Save follow-up'}</button></div></form>}{rows.length?<div className="followup-list">{rows.map(row=><article key={row.id}><div><strong>{String(row.follow_up_date).slice(0,10)}</strong><span className={`followup-status ${row.status.toLowerCase().replaceAll(' ','-')}`}>{row.status}</span></div><p>{row.notes}</p><small>{row.author} · {new Date(row.created_at).toLocaleString()}</small></article>)}</div>:!show&&<p className="followup-empty">{ur?'ابھی کوئی فالو اپ نہیں۔':'No follow-ups recorded yet.'}</p>}</section>}
