import {publicMetadata} from '../seo';
export const metadata=publicMetadata('Benefits of Connected Dairy Farm Management','Reduce paperwork, protect farm records, control costs, plan animal care, and make better dairy farm decisions with DairyMonitor.','/benefits');
export default function Layout({children}:{children:React.ReactNode}){return children}
