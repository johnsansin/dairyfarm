import {publicMetadata} from '../seo';
export const metadata=publicMetadata('Privacy Policy','Learn how DairyMonitor handles and protects account and dairy farm information.','/privacy');
export default function Layout({children}:{children:React.ReactNode}){return children}
