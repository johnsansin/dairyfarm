import {publicMetadata} from '../seo';
export const metadata=publicMetadata('Dairy Farm Software Pricing','Compare DairyMonitor plans for small, growing, and multi-farm dairy operations. Start with a 14-day free trial.','/pricing');
export default function Layout({children}:{children:React.ReactNode}){return children}
