import {publicMetadata} from '../seo';
export const metadata=publicMetadata('Terms of Service','Read the terms that apply when using the DairyMonitor dairy farm management platform.','/terms');
export default function Layout({children}:{children:React.ReactNode}){return children}
