import {publicMetadata} from '../seo';
export const metadata=publicMetadata('Cookie Policy','Learn how DairyMonitor uses cookies to operate and improve the dairy farm management platform.','/cookies');
export default function Layout({children}:{children:React.ReactNode}){return children}
