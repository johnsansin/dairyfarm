import {publicMetadata} from '../seo';
export const metadata=publicMetadata('Dairy Farm Management Features','Track herd records, milk production, animal health, breeding, feed, inventory, staff, accounting, and reports in one dairy farm platform.','/features');
export default function Layout({children}:{children:React.ReactNode}){return children}
